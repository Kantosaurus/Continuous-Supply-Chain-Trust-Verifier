# =============================================================================
# SCTV Environment Configuration Example
# =============================================================================
# Copy this file to .env and customize for your environment.
#
# IMPORTANT: Never commit .env files with real credentials to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_USER=sctv
# REQUIRED: pick a strong password before running docker-compose up.
# Generate one with: openssl rand -base64 24
POSTGRES_PASSWORD=REPLACE_ME_WITH_A_STRONG_PASSWORD
POSTGRES_DB=sctv
POSTGRES_PORT=5432

# Full database URL (used by services). Keep the password in sync with POSTGRES_PASSWORD above.
DATABASE_URL=postgres://sctv:REPLACE_ME_WITH_A_STRONG_PASSWORD@localhost:5432/sctv

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# Port to expose the API server
API_PORT=3000

# REQUIRED: JWT secret for authentication. Must be a long, random, unpredictable value.
# Generate a secure secret with: openssl rand -hex 32
# (or `python -c "import secrets; print(secrets.token_hex(32))"`)
# Never reuse this across environments and never commit the real value.
SCTV_JWT_SECRET=REPLACE_ME_RUN_openssl_rand_hex_32

# Enable CORS (set to false in production if not needed)
SCTV_ENABLE_CORS=true

# Log format: "json" for structured logging, "pretty" for development
SCTV_LOG_FORMAT=json

# Rust log level configuration
RUST_LOG=info,sctv_api=debug,sctv_worker=debug,tower_http=info

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
# Number of concurrent worker threads
SCTV_WORKER_COUNT=4

# Job queue polling interval (milliseconds)
SCTV_POLL_INTERVAL_MS=1000

# Graceful shutdown timeout (seconds)
SCTV_SHUTDOWN_TIMEOUT_SECS=30

# -----------------------------------------------------------------------------
# Dashboard Configuration (Optional)
# -----------------------------------------------------------------------------
DASHBOARD_PORT=8080

# -----------------------------------------------------------------------------
# Email/SMTP Configuration (for notifications)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@sctv.local

# Use TLS for SMTP connection
SMTP_TLS=true

# -----------------------------------------------------------------------------
# External Integrations (Optional)
# -----------------------------------------------------------------------------
# GitHub token for enhanced API access
GITHUB_TOKEN=

# GitLab token for integration
GITLAB_TOKEN=

# Sigstore/Fulcio for provenance verification
SIGSTORE_ENABLED=true
